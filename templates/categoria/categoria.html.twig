{% extends 'base.html.twig' %}

{% block title %}{{controller_name}}{% endblock %}

{% block body %}
    <div class="animated fadeIn">

        <div class="jumbotron jumbotron-fluid mt-4">
            <div class="container text-center">
                <h1 class="display-4">Categorías</h1>
            </div>
        </div>

        <hr class="mb-4">

        <div class="list-group">
            {% for c in data_categorias %}
                <a href="{{path('producto_cat', {'id': c.id})}}" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">{{c.nombre}}
                    <span class="badge badge-primary badge-pill">{{c.num / 8}}</span>
                </a>
            {% endfor %}
        </div>

    </div>

{% endblock %}
